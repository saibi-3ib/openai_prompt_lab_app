# This file makes 'app' a Python package so imports like `from app.admin_worker import admin_bp`
# can be resolved by IDEs and the Python import system.
# It's intentionally minimal. You can add package-level imports here later if desired.
